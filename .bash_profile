export PATH=/usr/local/bin:$PATH
export EDITOR=vim
export HISTCONTROL=ignoredups
#export HOMEBREW_GITHUB_API_TOKEN= #fill this in with something real if you get rate limited by homebrew

source $HOME/.bash/source_all

#eval "$(rbenv init -)" # if you need ruby and have installed rbenv
#export JAVA_HOME= #you'll probably want to set this to the latest javer

